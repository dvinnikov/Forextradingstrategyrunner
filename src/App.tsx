import { ForexStrategyRunner } from "./components/ForexStrategyRunner";

export default function App() {
  return (
    <div className="size-full bg-background">
      <ForexStrategyRunner />
    </div>
  );
}
